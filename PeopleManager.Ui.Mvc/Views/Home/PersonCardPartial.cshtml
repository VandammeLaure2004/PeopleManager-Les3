@model Person

<div class="card">
    <div class="card-header">
        <h2>@Model.FirstName @Model.LastName</h2>
    </div>
    <div class="card-body">
        <a class="btn btn-outline-primary" href="mailto:@Model.Email">Contact</a>
    </div>
</div>